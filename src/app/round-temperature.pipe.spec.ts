import { RoundTemperaturePipe } from './round-temperature.pipe';

describe('RoundTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
